package one;
import java.io.Serializable;
public class Hero {
	String name;
	int hitPoint;
	int damage;
	
	public Hero(String name, int hitPoint, int damage) {
		super();
		this.name = name;
		this.hitPoint = hitPoint;
		this.damage = damage;
	}
	public void AttackHero(Hero target) {
		try{Thread.sleep(1000);}
		catch (InterruptedException e) {
            // TODO Auto-generated catch block
            e.printStackTrace();
        }
		target.hitPoint-=this.damage;
		System.out.format("%s ÕýÔÚ¹¥»÷ %s \n",this.name,target.name);
		if (target.isDead()) {System.out.println(target.name +"ËÀÁË£¡");}
	}
	public boolean isDead() {
		if (this.hitPoint<=0) {return true;}
		else {return false;}
	}
}
